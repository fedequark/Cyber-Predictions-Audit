{
  "name": "cyber-predictions-audit",
  "version": "1.3.0",
  "private": true,
  "scripts": {
    "build:site": "node scripts/build-site.mjs",
    "analyze:research": "python scripts/analizar_robustez_v1_2.py && python scripts/diagnosticos_adicionales_v1_3.py",
    "check:research": "npm run analyze:research && node scripts/build-site.mjs",
    "deploy:cloudflare": "npm run build:site && npx wrangler@latest pages deploy dist --project-name cyber-predictions-audit --branch main"
  }
}
